spring:
  datasource:
    url: jdbc:mysql://mysql:3306/meu_banco?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: meu_usuario
    password: minha_senha
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
